# {{project_name}}

{{project_description}}

- Auto-generated [schema documentation](elements/index.md)
{%- if add_lokf %}
- LOKF knowledge base under `knowledge/` — markdown concepts that are also an
  RDF graph (build and serve with the `lokf-*` just recipes)
{%- endif %}
